<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.ragedunicorn.wow.classic</groupId>
  <artifactId>gearmenu-addon</artifactId>
  <version>2.6.0</version>
  <packaging>pom</packaging>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <addon.author>Michael Wiesendanger &lt;michael.wiesendanger@gmail.com&gt;</addon.author>
    <addon.description>Addon to manage and keybind usable gear</addon.description>
    <addon.name>GearMenu</addon.name>
    <addon.displayName>gearmenu</addon.displayName>
    <addon.curseforge.projectId>345496</addon.curseforge.projectId>
    <!--
      CurseForge game version IDs (not WoW interface numbers)

      Classic - covers: Classic Era, Hardcore, Season of Discovery
      Anniversary TBC - covers: TBC Anniversary Edition
      MoP Classic - covers: Mists of Pandaria Classic
    -->
    <!-- renovate: datasource=custom.wow-game-versions-classic depName=wow-gameversion-classic versioning=loose -->
    <addon.curseforge.gameVersionClassic>14029</addon.curseforge.gameVersionClassic>
    <!-- renovate: datasource=custom.wow-game-versions-tbcc depName=wow-gameversion-tbcc versioning=loose -->
    <addon.curseforge.gameVersionTbcc>14300</addon.curseforge.gameVersionTbcc>
    <!-- renovate: datasource=custom.wow-game-versions-mop depName=wow-gameversion-mop versioning=loose -->
    <addon.curseforge.gameVersionMop>16168</addon.curseforge.gameVersionMop>
    <addon.wago.projectId>kaNeybN2</addon.wago.projectId>
    <addon.name.short>GM</addon.name.short>
    <addon.tag.version>v${project.version}</addon.tag.version>
    <!-- renovate: datasource=custom.wow-versions-classic depName=wow-interface-classic versioning=loose -->
    <addon.interface.classic>11508</addon.interface.classic>
    <!-- renovate: datasource=custom.wow-versions-tbcc depName=wow-interface-tbcc versioning=loose -->
    <addon.interface.tbcc>20505</addon.interface.tbcc>
    <!-- renovate: datasource=custom.wow-versions-mop depName=wow-interface-mop versioning=loose -->
    <addon.interface.mop>50504</addon.interface.mop>
    <!-- Aggregated interface line consumed by the .toc template. -->
    <addon.interface>${addon.interface.classic}, ${addon.interface.tbcc}, ${addon.interface.mop}</addon.interface>
    <!--
      Wago patch versions.
      Classic - covers: Classic Era, Hardcore, Season of Discovery
      TBC - covers: TBC Anniversary (Burning Crusade Classic)
      MoP - covers: Mists of Pandaria Classic
    -->
    <!-- renovate: datasource=custom.wow-patch-versions-classic depName=wow-patch-classic versioning=loose -->
    <addon.supported.patch.classic>1.15.8</addon.supported.patch.classic>
    <!-- renovate: datasource=custom.wow-patch-versions-tbcc depName=wow-patch-tbcc versioning=loose -->
    <addon.supported.patch.tbcc>2.5.5</addon.supported.patch.tbcc>
    <!-- renovate: datasource=custom.wow-patch-versions-mop depName=wow-patch-mop versioning=loose -->
    <addon.supported.patch.mop>5.5.4</addon.supported.patch.mop>
    <addon.identifier>${project.groupId}.${project.artifactId}</addon.identifier>
    <addon.project.categories>Action Bars Combat PvP</addon.project.categories>
  </properties>

  <profiles>
    <profile>
      <id>development</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <properties>
        <profile.id.value>development</profile.id.value>
        <package.descriptor>${project.basedir}/build-resources/assembly-development.xml</package.descriptor>
        <package.filter>${project.basedir}/build-resources/addon-development.properties</package.filter>
        <package.finalName>${addon.name}</package.finalName>
        <package.appendAssemblyId>true</package.appendAssemblyId>
        <package.includeBaseDirectory>false</package.includeBaseDirectory>
        <generate.sources.skip>false</generate.sources.skip>
        <generate.sources.filter>${project.basedir}/build-resources/addon-development.properties</generate.sources.filter>
        <generate.sources.descriptor>${project.basedir}/build-resources/generate-development.xml</generate.sources.descriptor>
        <generate.sources.overwrite>false</generate.sources.overwrite>
      </properties>
    </profile>
    <profile>
      <id>release</id>
      <properties>
        <profile.id.value>release</profile.id.value>
        <package.descriptor>${project.basedir}/build-resources/assembly-release.xml</package.descriptor>
        <package.filter>${project.basedir}/build-resources/addon-release.properties</package.filter>
        <package.finalName>${addon.name}</package.finalName>
        <package.appendAssemblyId>false</package.appendAssemblyId>
        <package.includeBaseDirectory>false</package.includeBaseDirectory>
        <generate.sources.skip>false</generate.sources.skip>
        <generate.sources.filter>${project.basedir}/build-resources/addon-release.properties</generate.sources.filter>
        <generate.sources.descriptor>${project.basedir}/build-resources/generate-release.xml</generate.sources.descriptor>
        <generate.sources.overwrite>false</generate.sources.overwrite>
      </properties>
    </profile>
    <profile>
      <id>deploy-github</id>
      <properties>
        <profile.id.value>deploy</profile.id.value>
        <package.descriptor>${project.basedir}/build-resources/assembly-release.xml</package.descriptor>
        <package.filter>${project.basedir}/build-resources/addon-release.properties</package.filter>
        <package.finalName>${addon.name}</package.finalName>
        <package.appendAssemblyId>false</package.appendAssemblyId>
        <package.includeBaseDirectory>false</package.includeBaseDirectory>
        <generate.sources.skip>true</generate.sources.skip>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>com.ragedunicorn.tools.maven</groupId>
            <artifactId>github-release-maven-plugin</artifactId>
            <version>1.0.7</version>
            <executions>
              <execution>
                <id>default-cli</id>
                <phase>package</phase>
                <goals>
                  <goal>github-release</goal>
                </goals>
                <configuration>
                  <owner>ragedunicorn</owner>
                  <repository>wow-classic-gearmenu</repository>
                  <!--suppress UnresolvedMavenProperty -->
                  <authToken>${github.auth-token}</authToken>
                  <tagName>${addon.tag.version}</tagName>
                  <name>${addon.tag.version}</name>
                  <releaseNotes>${project.basedir}/build-resources/release-notes.md</releaseNotes>
                  <assets>
                    <asset>${project.basedir}/target/${addon.name}.zip</asset>
                  </assets>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>deploy-curseforge</id>
      <properties>
        <profile.id.value>deploy</profile.id.value>
        <package.descriptor>${project.basedir}/build-resources/assembly-release.xml</package.descriptor>
        <package.filter>${project.basedir}/build-resources/addon-release.properties</package.filter>
        <package.finalName>${addon.name}</package.finalName>
        <package.appendAssemblyId>false</package.appendAssemblyId>
        <package.includeBaseDirectory>true</package.includeBaseDirectory>
        <generate.sources.skip>true</generate.sources.skip>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>com.ragedunicorn.tools.maven</groupId>
            <artifactId>curseforge-release-maven-plugin</artifactId>
            <version>2.0.1</version>
            <executions>
              <execution>
                <id>default-cli</id>
                <phase>package</phase>
                <goals>
                  <goal>curseforge-release</goal>
                </goals>
                <configuration>
                  <game>wow</game>
                  <projectId>${addon.curseforge.projectId}</projectId>
                  <displayName>${addon.displayName}-${addon.tag.version}.zip</displayName>
                  <changelogFile>${project.basedir}/build-resources/release-notes.md</changelogFile>
                  <changelogType>markdown</changelogType>
                  <gameVersions>
                    <gameVersion>${addon.curseforge.gameVersionClassic}</gameVersion>
                    <gameVersion>${addon.curseforge.gameVersionTbcc}</gameVersion>
                    <gameVersion>${addon.curseforge.gameVersionMop}</gameVersion>
                  </gameVersions>
                  <releaseType>release</releaseType>
                  <file>${project.basedir}/target/${addon.name}.zip</file>
                  <!--suppress UnresolvedMavenProperty -->
                  <authToken>${curseforge.auth-token}</authToken>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>deploy-wago</id>
      <properties>
        <profile.id.value>deploy</profile.id.value>
        <package.descriptor>${project.basedir}/build-resources/assembly-release.xml</package.descriptor>
        <package.filter>${project.basedir}/build-resources/addon-release.properties</package.filter>
        <package.finalName>${addon.name}</package.finalName>
        <package.appendAssemblyId>false</package.appendAssemblyId>
        <package.includeBaseDirectory>true</package.includeBaseDirectory>
        <generate.sources.skip>true</generate.sources.skip>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>com.ragedunicorn.tools.maven</groupId>
            <artifactId>wago-release-maven-plugin</artifactId>
            <version>2.1.1</version>
            <executions>
              <execution>
                <id>default-cli</id>
                <phase>package</phase>
                <goals>
                  <goal>wago-release</goal>
                </goals>
                <configuration>
                  <projectId>${addon.wago.projectId}</projectId>
                  <label>${addon.displayName}-${addon.tag.version}.zip</label>
                  <changelogFile>${project.basedir}/build-resources/release-notes.md</changelogFile>
                  <supportedClassicPatch>${addon.supported.patch.classic}</supportedClassicPatch>
                  <supportedBccPatch>${addon.supported.patch.tbcc}</supportedBccPatch>
                  <supportedMopPatch>${addon.supported.patch.mop}</supportedMopPatch>
                  <stability>stable</stability>
                  <file>${project.basedir}/target/${addon.name}.zip</file>
                  <!--suppress UnresolvedMavenProperty -->
                  <authToken>${wago.auth-token}</authToken>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>3.8.0</version>
        <executions>
          <execution>
            <id>default-cli</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <descriptors>
                <descriptor>${package.descriptor}</descriptor>
              </descriptors>
              <filters>
                <filter>${package.filter}</filter>
              </filters>
              <finalName>${package.finalName}</finalName>
              <appendAssemblyId>${package.appendAssemblyId}</appendAssemblyId>
            </configuration>
          </execution>
          <execution>
            <id>generate-sources</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <attach>false</attach>
              <skipAssembly>${generate.sources.skip}</skipAssembly>
              <descriptors>
                <descriptor>${generate.sources.descriptor}</descriptor>
              </descriptors>
              <filters>
                <filter>${generate.sources.filter}</filter>
              </filters>
              <finalName>generated</finalName>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <id>default-cli</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <skip>${generate.sources.skip}</skip>
              <outputDirectory>${project.basedir}</outputDirectory>
              <overwrite>${generate.sources.overwrite}</overwrite>
              <resources>
                <resource>
                  <directory>${project.basedir}/target/generated-${profile.id.value}</directory>
                  <targetPath>${project.basedir}</targetPath>
                  <filtering>false</filtering>
                  <includes>
                    <include>GearMenu.toc</include>
                  </includes>
                </resource>
                <resource>
                  <directory>${project.basedir}/target/generated-${profile.id.value}</directory>
                  <targetPath>${project.basedir}/code</targetPath>
                  <filtering>false</filtering>
                  <includes>
                    <include>Environment.lua</include>
                  </includes>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
